[build]
builder = "nixpacks"

[build.nixpacks]
install_cmd = "npm ci"
build_cmd = "npm run build && npx drizzle-kit push"

[deploy]
startCommand = "npm run start"
healthcheckPath = "/health"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
